@echo off
echo This script is deprecated. Shader compilation is now handled by CMake.
echo Run 'cmake --build build' to compile all shaders automatically.
echo.
echo If you need manual shader compilation, use:
echo glslc src\<example>\<shader> -o build\<output>.spv
pause
